PLEASE NOTE:

I will not be providing a datcom output file or saved matricies to clean up the .zip folder I am submitting.
To create these files, do the following:

1 - Unzip this entire folder.  You will need all files in each respective folder to run without errors

2 - run 'DatcomImport.m' , entering 'liepke.dat' when prompted.  This will run datcom and save the matricies

3 - open and run 'LiepkePlane.slx', which will automaticially run 'runStart', run the simulation, and display 
		a graph of the flight.

4 - If you want to test FlightGear, modify the 'FG.bat' file and uncommment the corresponding blocks in simulink